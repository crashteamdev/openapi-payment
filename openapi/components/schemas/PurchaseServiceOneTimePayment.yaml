description: Оплата сервиса с выставлением счета пользователю
required:
  - successRedirectUrl
  - failRedirectUrl
  - provider
allOf:
  - $ref: ./PurchaseService.yaml
  - properties:
      successRedirectUrl:
        type: string
        description: Адрес перенаправления при успешном прохождении платежа
      failRedirectUrl:
        type: string
        description: Адрес перенаправления при неуспешном прохождении платежа
      savePaymentMethod:
        type: boolean
      provider:
        $ref: ./CreatePaymentProvider.yaml
