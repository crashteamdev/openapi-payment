description: KazanExpress аналитика
required:
  - plan
allOf:
  - $ref: ./PaidService.yaml
  - properties:
      plan:
        type: string
        enum:
          - default
          - advanced
          - pro

