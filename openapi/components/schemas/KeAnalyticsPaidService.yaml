description: KazanExpress аналитика
required:
  - plan
allOf:
  - $ref: ./PurchaseService.yaml
  - properties:
      plan:
        type: string
        enum:
          - default
          - advanced
          - pro

