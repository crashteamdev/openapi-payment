type: object
required:
  - provider
discriminator:
  propertyName: provider
  mapping:
    freekassa: ./FreekassaPaymentProvider.yaml
    uz-click: ./UzClickPaymentProvider.yaml
properties:
  provider:
    type: string
    enum:
      - freekassa
      - uz-click
